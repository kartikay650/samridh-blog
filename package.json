{
  "name": "samridh-blog",
  "version": "0.1.0",
  "private": true,
  "description": "GEO-optimized blog generation engine — grounded in your brand-truth, produces AI-citable markdown posts.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "tsx run.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
